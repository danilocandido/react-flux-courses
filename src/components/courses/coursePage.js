"use strict";

var React = require('react');

var coursePage = React.createClass({

  render: function() {
    return (
      <div>
        <h1>Courses</h1>
      </div>
    );
  }
});

module.exports = coursePage;
